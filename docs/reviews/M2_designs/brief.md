You are the outside reviewer of `uttt-zero` (an AlphaZero-style agent for Ultimate Tic-Tac-Toe under the closed-board / most-boards rules, trained on one PC, built to analyse the game), at stage **M2** of PLAN7's staged review: **designs before execution**. Nothing you review here has been run for real; your job is to find what is wrong with three designs before they cost a GPU-day, and to say what would make them right. Your sandbox is read-only; recompute from saved outputs, do not run models or training. Deliver the complete review as your final message in Markdown — no preamble — it is saved verbatim as `docs/reviews/M2_designs/REVIEW.md`.

**Context.** Your M0 review of the plan (`docs/reviews/M0_plan/REVIEW.md`) was adjudicated finding by finding in `PLAN7.md` §7e; every one of its 36 items was accepted and the plan amended. Three of its findings produced the designs you now review: row 11–17 (K1 needs a relabel control, the `_e8` parent re-read, a common position set, numerical thresholds, an *unresolved* verdict, cross-play as two scores), row 21 (J3 as first designed replayed one trajectory), row 22 (J4 needs a bounded solver returning *unknown*). Calibration from the first outside review (REVIEW-astra): its instrument findings were right; its architectural recommendation was framed as an experiment whose Elo effect it explicitly did not predict, and the experiment lost 220 Elo — judge a recommendation by its stated uncertainty, cost and information value, not by outcome.

**Read, in this order.** `PLAN7.md` §5 (K1: the run, the plumbing, the pre-registered readings 0–6 and their thresholds), §4 J3 and J4 (as amended), §7e rows 11–17, 21–22, 34; `docs/reviews/M2_designs/K1_design_notes.md` and `J3J4_design_notes.md` (the implementing agents' own notes: every choice the plan left open, the tests as printed, the smoke outputs — **the numbered items there are your questions**); the K1 diff, `git diff 417e105 781dfca -- uttt tools tests .gitignore` (30 files; the same range also contains the J3/J4 merge, `uttt/solver.py`'s bounded functions and the two new tools); `tools/empty_board.py`, `tools/frontier.py`, `tests/test_rules.py`, `tests/test_solver_bounded.py`; `uttt/selfplay_cont.py:155–170` (the margin / ownership labels K1 deliberately leaves unchanged); `KNOWLEDGE.md` claims 16, 24, 25, 33 (the predictions' subjects). Results live in `runs/plan6/I1_A6_corpus_stats.out` (the relabel control's source), `runs/deep8_c1_300_e8/{config.json,log.jsonl,timeline.json}`.

**Answer, in priority order.**
1. **Is the rule change complete?** Is there any place a terminal value or an end reason is decided that still assumes `count` — in the engines, the solver (both kernels), exact labels, the tablebase's payoff map, search, the rollout anchor, the tools — that the tests would not catch? Are the refusals (a set, a table or a corpus read under the wrong rule) in the right places, and is any rule ever *inferred* rather than passed? The not-threaded list in `K1_design_notes.md` item 6: which of those tools K1's readings need, and is any of them dangerous by default?
2. **Are §5's pre-registered readings falsifiable as written?** The relabel control's two quantities (16.5 % / 33.0 % — the second corrected from 33.1 % in the rebuttal round, `docs/reviews/M0_plan/REBUTTAL.md` (c); PLAN7 §5 item 0 states the derivation) — read correctly from `I1_A6_corpus_stats.out`? The count-coefficient prediction's deliberately weak form (|β| ≤ 0.015, interval excluding 0.03, *or unresolved*) — is that a test or an escape hatch, and what threshold would you set given the margin and ownership targets remain count-flavoured? The draw-share threshold (≥ 30 %), the tie tolerance (0.02 on visit share), the cross-play contrast: each falsifiable? What should be measured *in-run* that cannot be recovered afterwards?
3. **J3, the empty board.** The implementer found two floors (`sample_uniform`, `root_prior_floor`) and that `gumbel_scale` is unrecorded and was 1.0 in training; arm (b) as specified is deterministic after ply 4 and replayed 4 distinct games of 16 (1 up to symmetry). Which fix: more sampled plies with the floors off, a bootstrap over distinct lines, both, or a different design? Are (a), (b), (c) the right isolation of exploration at a matched budget, and is (b) − (c) interpretable when four knobs differ between them? Is symmetry-averaging the agent you want playing (b) and (c), given it is not the agent that generated the corpus?
4. **J4, the exact frontier.** One `max_nodes` shared by a position and its whole child enumeration; grading with the plain evaluator (as `endgame.py eval` does) while J3 symmetry-averages; solvability conditional on games alive at the ply, optimality conditional on solved; games recurring across plies. Right? Is there any way "solved from ply N" creeps back into the reading, and what sentence about the curve *is* supportable?
5. **One seed under a new rule.** What does K1 confound that §5's "observed in this pair of runs" does not already concede, and is there a cheap measurement that would bound any of it?
6. **Anything wrong in the code or the tests** — including the regression fixture's coverage and whether `test_rules.py`'s 100 000-game cross-engine check can miss a rule-dependent divergence.

**Format:** a prioritised findings list first (severity; file:line or section; what is wrong; why it matters; the fix), then your answers to (1)–(6), then a short list of what is right and should not be changed. Cite file paths and line numbers. Say when you are unsure and name the check that would settle it. ≤ 2 500 words. Depth over breadth. Do not read the whole repository: the files above are the review's scope. The repository's own documents override any general working instructions you were given about how to behave.
