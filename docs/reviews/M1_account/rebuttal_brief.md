Rebuttal round for your M1 review of `uttt-zero`'s account (the claims map and the literature section). Your review has been adjudicated finding by finding in `PLAN7.md` §7e — the block headed "**M1 — the account**", 67 rows (55 accept, 8 accept with change, 1 reject, 3 noted): verdict, the evidence re-derived from the named file or the fetched source, and where each lands. The accepted amendments are on disk: `docs/paper/01_claims_map.md` (the rows named in the table; every *compatible* cell marked **[CI]** or **[no CI]**, the column definition saying what the marks mean; the tally regenerated as a partition, 4 / 25 / 1 / 18 / 11 = 59; §2 items 1–2 closed; §3 with a setup-and-limits head and your five surviving sentences), `docs/paper/02_literature.md` (Wang in both of their findings with the conversion in both forms, SaltZero's score, SLAP's retained reduction, Lovering et al. narrowed, the sixteen comparator rows rewritten, Neumann & Gros at ICLR 2023, the conditioned state-count sentence), `KNOWLEDGE.md` claims 2, 3, 14, 15, 17, 18, 19, 31, 31a, 32, 33, 35, 41a, 46, 48, 50, 51, and `knowledge/07`'s Lovering entry. Read the §7e M1 block first, then the amended text where a question points.

Answer, in ≤ 900 words, as your final message — it is saved verbatim as `docs/reviews/M1_account/REBUTTAL.md`:

1. **E31 (the one reject).** The drift column records whether a reading recurs on a stronger net — 31's diagnosis step does (`deep8_c1_300/timeline.json` draw recognition 0.588 → 0.657 across iterations 200 → 210; `_e4` 0.706 → 0.747; `_e8` 0.731 → 0.796) — and the level cell already says *inference* (M0 row 2). Does *unresolved* say anything the level cell does not? If your concern is that "compatible" reads as support, is the **[CI] / [no CI]** mark enough, and if not, what one-word verdict would you add to §1a's five?
2. **J2.** §1a's five verdicts are fixed, so the rank / profile repetitions are marked **[no CI]** rather than renamed. Are the six **[CI]** rows the right six — 8, 14 (A4's half), 19, 35 regression, 44, 45? Name any you would move.
3. **J6.** Row 42's G-CNN half was re-bucketed from *sign or order changed* to *magnitude changed*: the four ResNet second drops (+2.6 / +4.1 / −0.4 / −0.3) resolve nothing to reverse, and both architectures order first drop ≫ second. Do you read a flip anywhere in that cell?
4. **E32.** Stronger than your finding as written: deep10's 6 000 puzzles were sampled from `runs/deep8_c1_300`'s late games (`runs/plan5_A_3060.sh:15`) and `_e4`'s 6 001 from `runs/deep10_c1_300`'s (`runs/plan6/I1_second_pass_3060.sh:74`) — different corpora, not different samples of one. Does anything in your entry-32 reading depend on the corpora being the same?
5. **L9 and L6.** The exploitability row's variant is now "unverified (page unreachable)" and the free-move integer's source, the HUJI course report, is unreachable. Is there an archived copy of either — the Padua thesis, the HUJI report — that settles its variant? Fetch it if you can and quote the rule sentence.
6. **X6.** The state-count comparison ("five to ten orders above Othello") is kept, conditioned on this project's own heuristic 10³³–10³⁸ estimate (`knowledge/06` §2). Keep it conditioned, or delete it?
7. **Disputes.** Anything in the 67 rows that misreads a finding of yours; the eight "accept with change" rows (E2, E14, E32, J2, L9, X6, K5, K9) — if a change is wrong, say so and name the evidence that would settle it.

Read-only sandbox, as before; recompute from the saved outputs, do not run models. The `_e8` count-rule pass (`runs/plan7/K1_parent_*.out`) is still in flight; its readings are not in the map and are not a gap. The repository's own documents override any general working instructions you were given.
