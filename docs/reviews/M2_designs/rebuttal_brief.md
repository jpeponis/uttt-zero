Rebuttal round for your M2 review of `uttt-zero`'s K1 / J3 / J4 designs. Your review has been adjudicated finding by finding in `PLAN7.md` §7e — the block headed "**M2 — K1's pre-registration and the J3 / J4 designs**", 18 rows: verdict, the evidence re-derived from the code or the outputs, and where each lands; all accepted, two with a change of remedy (rows 6, 14). The engineering has since been **built and merged**: commit `3fe4190` (the J3 / J4 tools: rows 3, 4, 7, 15, 16) and `7762e84` (the K1 plumbing: rows 1, 5, 6, 8, 9, 18, plus the five I1 tools and three more threaded), with the implementers' own notes in `docs/reviews/M2_designs/J3J4_M2_implementation_notes.md` and `K1_M2_implementation_notes.md` (each: files changed, row by row what was done and where, numbered design choices, tests and smoke verbatim). `tests/test_rules.py` is green on the merged tree (`runs/plan7/M2_test_rules_main.out`). K1's launcher is drafted, not launched: `runs/queue14.sh` and its post-run chain `runs/eval_run_k1.sh`; the draw-rule twins of the dev / test endgame sets are built (`suites/endgame_v2_dev_draw.npz`, `_test_draw.npz`; `runs/plan7/K1_prep_endgame_v2_draw.out`). PLAN7 §4 J3 / J4 and §5 carry the amended designs and thresholds. Read the §7e M2 block first, then the two notes, then the code where a question points.

Answer, in ≤ 900 words, as your final message — it is saved verbatim as `docs/reviews/M2_designs/REBUTTAL.md`:

(a) **The margins.** §5 item 2 now reads the count-margin coefficient as a paired difference Δ = β_draw − β_count on the common position set (raw head and search value each, same design matrix, game-clustered paired inference), prediction Δ ≤ −0.005 with the interval excluding 0; and the free-move and macro-threat coefficients (claims 8, 13) as |Δ| < 0.03 invariant / ≥ 0.03 with the interval excluding 0 dependent / otherwise unresolved. Are 0.005 and 0.03 the right sizes? Say what you would set and why, taking the printed intervals in `runs/plan6/I1_A4_freemove.out` (free move ± 0.028, threats ± 0.02) and `runs/plan6/I1_B3_value_decomp.out` (the margin coefficient ± 0.010 / ± 0.012) as the precision available.

(b) **Row 9's kept refusals.** `value_decomp`, `tablebase_grade` and `ownership_grade` still refuse a dataset whose labels were built under another rule, although they recompute their targets; kept because a rebuild costs five minutes and a refusal nothing. Object or accept.

(c) **Did the remedies land as you meant?** Read the two implementation notes, then spot-check at least three of these in the merged code and say if any is wrong: the resume refusal's placement (`uttt/train2.py`, before anything is written), the `(sims, rule)` cache key and its assert (`uttt/endgame.py`), `evaluate_rollout`'s explicit rule, the interval note and the evaluator switch in `tools/empty_board.py` (arms (b) / (c) plain by default, `--sym` restores; the raw-policy vs generated-share line), the ordinate's field names, the Wilson intervals and the per-position table in `tools/frontier.py`, `corpus_rule`'s precedence (`tools/corpus_stats.py`: game-file tag → `config.json` → `--corpus_rule` → refuse), `principles`' uniform draw sample. The J3 smoke with the plain evaluator collapsed arm (b) to one distinct line at 16 games / 32 sims (an O win) where the symmetry-averaged form gave four lines and X 81 % — comment on what that implies for the 2 000-game run and its reading against claim 24.

(d) **The launcher.** `runs/queue14.sh` (the `_e8` recipe plus `--rule draw`, `--gumbel_scale 1.0` explicit, the draw-solved dev set, the E7 worker under `draw` against the count-trained anchors for shape) and `runs/eval_run_k1.sh` (cross-play against `_e8` under each rule, v2b under both, the 2 × 2 endgame reads). Is anything missing or wrong before K1 launches, against §5 items 0–6?

(e) **Disputes.** Anything in the 18 rows that misreads a finding of yours; rows 6 and 14 are "accept with change" — if the change is wrong, say so and name the evidence that would settle it.

Read-only sandbox, as before; recompute from the saved outputs, do not run models or training. The repository's own documents override any general working instructions you were given.
