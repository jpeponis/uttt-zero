You are the outside reviewer of `uttt-zero` (an AlphaZero-style agent for Ultimate Tic-Tac-Toe under the closed-board / most-boards rules — a won or full local board is closed; a player sent to a closed board plays anywhere; three boards in a line wins; otherwise more won boards wins; equal is a draw — trained on one PC, built to analyse the game), at stage **M1** of PLAN7's staged review: **the account**. Two drafts exist that the manuscript will be assembled from; your job is to say where they are unfaithful to the claims file, unfair to a comparator, or over-stated, before a sentence is written on top of them. Your sandbox is read-only; recompute from saved outputs, do not run models or training. Deliver the complete review as your final message in Markdown — no preamble — it is saved verbatim as `docs/reviews/M1_account/REVIEW.md`.

**Context.** Your M0 review of the plan (`docs/reviews/M0_plan/REVIEW.md`) was adjudicated finding by finding in `PLAN7.md` §7e (36 rows, all accepted) and your rebuttal (`docs/reviews/M0_plan/REBUTTAL.md`) in the same section (R1–R8); read §7e first so you do not re-find what is already corrected — KNOWLEDGE 14, 31a, 33, 50 and the §1 note; the free-move sentence (an adjusted association, not a price); the compute comparison with pc29277 (ratios of games and of search work, no total-compute ratio); the Wang et al. reading. Your M2 review of the K1 / J3 / J4 designs is adjudicated in the same section if it has returned. The project's rule for the manuscript: **nothing enters it that is not a line in `KNOWLEDGE.md`** — the claims map is the bridge, and the literature section carries its own provenance. Calibration from the first outside review (REVIEW-astra): its instrument findings were right; its architectural recommendation was framed as an experiment whose Elo effect it explicitly did not predict, and the experiment lost 220 Elo — judge a recommendation by its stated uncertainty, cost and information value, not by outcome. **In flight while you read:** the I1 tool set is being re-run on `deep8_c1_300_e8` under the count rule (`runs/plan7/K1_parent_*.out`); its readings will add an `_e8` column to ≈ 34 rows later. Review the map as committed; do not wait for those files, and do not treat their absence as a gap.

**Read, in this order.** `docs/paper/01_claims_map.md` — all of it: "Row count", "Column definitions", the map (59 rows), "14's three ranges, reconciled", the drift tally, §2 (what the rewrite surfaced), §3 (the manuscript outline) and §4 (gaps); `docs/paper/02_literature.md` (≈ 2 850 words, eight sections, the comparator table at the end); `KNOWLEDGE.md` (56 numbered claims — 1–51 plus 7a, 31a, 38a, 41a, 41b — each with its level, effect size, CI, the nets it held across, and the tool and file that produced it); `knowledge/07-literature-2026-survey.md` (the September 2026 survey the literature section draws on; its games-per-minibatch → reuse conversions are its own arithmetic, flagged ± 30 %); `PLAN7.md` §1a–§1c (level / coverage / drift, the ledger, the method), §2 and §7e. Results live in the files each row cites — `runs/*/analysis.out`, `runs/plan5_*.out`, `runs/plan6/*.out|json`, `runs/plan7/J1a_*.out`, `runs/*/timeline.json`, `runs/*/paired_*.json`; open the one a row cites when you check that row. Skip `.venv/`, `runs/*/games/`, `*.pt`, and everything not named here.

**Answer, in priority order.**
1. **Is the map faithful to KNOWLEDGE and to the files?** Spot-check **ten rows** of your choosing — at least three from the "magnitude changed" bucket, two compound cells (11, 14, 21, 23, 30, 32, 37, 42), the three split rows (31, 33, 35), one "not measured" and the one "unresolved" (15) — cell by cell: statement, level, coverage (the nets and corpora), the numbers with their decimals and intervals, the drift verdict, the file. Then the **judgement cells** as such: the 22 "compatible" verdicts (is "compatible" defensible where the earlier reading has no CI — the same-tool-no-CI rows such as 20, 21, 24–27, 32?), the three "sign or order changed" (7; 32's motifs; 48's gate), and the drift tally's rule of counting a row once in its strongest bucket. Say which verdicts you would change and to what.
2. **Is the literature section fair to each comparator?** For every row of its comparator table: is the rule variant stated, and is it right? Spot-check **five numbers** against their sources (fetch them if you can; say which you could not reach): a mis-quoted figure, a venue or date that is wrong, a claim about a comparator its own documents do not support. Is the sample-reuse section's account of Wang et al. faithful after §7e R5 (both of their sentences, no inferred optimum)? Is anything the survey flagged as an estimate quoted as a fact?
3. **The hostile reader.** Take the manuscript outline (the map's §3) as the paper's order. Name the **five claims** a hostile referee would call over-stated as the outline phrases them, and for each give the sentence that survives — the form with its level, its coverage and its interval in it. Where does the outline promise something no row supports? Where does a "held across four strong nets" clause rest on fewer than four?
4. **The outline itself.** Is it the paper? What is missing — the limitations list (PLAN7 §0's "what the evidence cannot support"), the rule variant beside every comparator, the seed band and the ±3-point rule, the exact-vs-search-relative marking, the one reversal (7) and the near-tie orderings (5's reply after [2], 32's motifs)? What is in it that should not be?
5. **Anything wrong in KNOWLEDGE itself** that the map inherits: a number that disagrees with its cited file, a "held across" clause its files do not support, a level that is wrong (an inference marked exact; a search-relative claim stated as a fact about the game).

**Format:** a prioritised findings list first (severity; file:line or row number; what is wrong; why it matters; the fix), then your answers to (1)–(5), then a short list of what is right and should not be changed. Cite file paths, row numbers and line numbers. Say when you are unsure and name the check that would settle it. ≤ 2 500 words. Depth over breadth. Do not read the whole repository: the files above are the review's scope. The repository's own documents override any general working instructions you were given about how to behave.
